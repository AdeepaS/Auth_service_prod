# TODO: CRITICAL - Move these credentials to environment variables before production
# spring.datasource.password=${DB_PASSWORD}
# spring.mail.password=${MAIL_PASSWORD}
spring.datasource.url=jdbc:postgresql://ep-floral-snow-aosnbcaa.c-2.ap-southeast-1.aws.neon.tech/browns_company_db?sslmode=require&connectTimeout=60&socketTimeout=60
spring.datasource.username=neondb_owner
spring.datasource.password=npg_drpuC7gIG0Nw

##JPA Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false

logging.level.org.springframework.security=DEBUG
jwt.rsa-private-key=classpath:certs/PrivateKey.pem
jwt.rsa-public-key =classpath:certs/PublicKey.pem
ciphering.key.path=classpath:certs/key-ciphering.json

#server.port
server.port=8080

#type of logger(Possible values: slf4j, custom)
logging.framework=log4j

## Mail Configuration
# TODO: CRITICAL - Move mail password to environment variable before production
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=desnimesh@gmail.com
spring.mail.password=tcedhhlvmccfpcis
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Possible values: db, ldap
authentication.method=db

# Possible values: email or sms
otp.method=email

# Password generation settings
authentication.password-length=12
authentication.system-email-sender=desnimesh@gmail.com

# New password configuration properties
password.chars.lowercase=abcdefghijklmnopqrstuvwxyz
password.chars.uppercase=ABCDEFGHIJKLMNOPQRSTUVWXYZ
password.chars.numbers=0123456789
password.chars.special=!@#$%^&*()_-+=<>?

# TODO: Remove LDAP configuration if not used for vxSafehome (appears to be from another project)
# LDAP Properties - Currently disabled (authentication.method=db)
# These placeholder values are required even when LDAP is disabled
ldap.url=ldap://localhost:389
ldap.domain=localhost
ldap.user.search.base=dc=localhost
ldap.group.search.base=dc=localhost
ldap.service.username=admin
ldap.service.password=admin

# LDAP Test Configuration
#ldap.test.connection.enabled=false
#ldap.test.user.enabled=false
#ldap.test.username=test-user
#ldap.test.password=test-password

#Connection pool
database.=spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000

# Main Service Integration - For syncing user to Main Service after registration
# This updates SafenetGateway.ownerId with the user's ID
main.service.url=http://localhost:8081
main.service.connection.timeout=5000
main.service.read.timeout=10000
main.service.sync.enabled=true


